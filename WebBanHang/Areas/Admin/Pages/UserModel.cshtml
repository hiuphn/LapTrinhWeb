@page
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model WebBanHang.Areas.Admin.Pages.UserModel
@{
    ViewData["Title"] = "Quản lý tài khoản";
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
}

<div style="padding-top:200px;padding-left:50px">
<h1>Quản lý tài khoản</h1>
@* <table class="table">
    <thead>
        <tr>
            <th>Tên người dùng</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            
            <tr>
                <td>@user.FullName</td>
                <td>@user.Email</td>
                @if (!user.IsLocked)
                {
                    <td>
                        <a asp-page="banedModel" asp-route-id="@user.Id">
                            Khóa 
                        </a>
                        
                        
                    </td>
                }

            </tr>
        }
    </tbody>
</table> *@
    <div class="row">
        <div class="col-lg-12">
            <div class="">
                <div class="table-responsive">
                    <table class="table project-list-table table-nowrap align-middle table-borderless">
                        <thead>
                            <tr> 
                                    <th scope="col">Tên</th>
                                    <th scope="col">Email</th>
                                    <th scope="col">UserName</th>
                                    <th scope="col">Khóa Tài Khoản </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Users)
                            {
                                <tr>

                                    <td>
                                        
                                        <p class="text-body">@item.FullName</p>
                                    </td>
                                    <td><span class="badge badge-soft-success mb-0">@item.Email</span></td>
                                    <td><span class="badge badge-soft-info mb-0">@item.UserName</span></td>
                                    <td>
                                        <ul class="list-inline mb-0" style="align-items:center">
                                            @if (!item.IsLocked)
                                            {
                                                <li class="list-inline-item">
                                                    <a asp-page="banedModel" asp-route-id="@item.Id" data-bs-toggle="tooltip" data-bs-placement="top" title="Khóa Tài khoản" class="px-2 text-warning"><i class="bi bi-file-lock font-size-30"></i>Khóa</a>
                                                </li>
                                            }

                                        </ul>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>



</div>