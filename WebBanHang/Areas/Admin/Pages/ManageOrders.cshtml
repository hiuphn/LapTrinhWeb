@page 
@using Microsoft.AspNetCore.Mvc.Rendering
@model WebBanHang.Areas.Admin.Pages.ManageOrders
@{
    ViewData["Title"] = "Quản lý đơn hàng";
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
}



<div style="padding-top: 200px;padding-left:50px">
<h1>Quản lý đơn hàng</h1>

<form method="get">
    <label for="date">Chọn ngày:</label>
    <input type="date" id="date" name="date" value="@DateTime.Today.ToString("yyyy-MM-dd")" />
        <button type="submit" class="btn btn-outline-warning btn-rounded" data-mdb-ripple-init data-mdb-ripple-color="dark"><i class="bi bi-eye"></i></i>Xem</button>
</form>

@* <table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Ngày đặt hàng</th>
            <th>Tổng giá</th>
            <!-- Các cột khác -->
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model.Orders)
        {
            <tr>
                <td>@order.Id</td>
                <td>@order.OrderDate</td>
                <td>@order.TotalPrice</td>
                <!-- Các cột khác -->
            </tr>
        }
    </tbody>
</table> *@
    <div class="row">
        <div class="col-lg-12">
            <div class="">
                <div class="table-responsive">
                    <table class="table project-list-table table-nowrap align-middle table-borderless">
                        <thead>
                            <tr>
                                <th scope="col">ID</th>
                                <th scope="col">Ngày Đặt Hàng</th>
                                <th scope="col">Tổng Giá</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Orders)
                            {
                                <tr>

                                    <td>

                                        <p class="text-body">@item.Id</p>
                                    </td>
                                    <td><span class="badge badge-soft-success mb-0">@item.OrderDate</span></td>
                                    <td><span class="badge badge-soft-danger mb-0">@item.TotalPrice</span></td>
                                    <td>
                                        <ul class="list-inline mb-0" style="align-items:center">
                                            

                                        </ul>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
</div>