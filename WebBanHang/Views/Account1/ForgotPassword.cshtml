@model WebBanHang.ViewModels.ForgotPasswordViewModel
@{
    ViewData["Title"] = "ForgotPassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@* <h1>Quên Mật Khẩu</h1>

<p>Vui lòng nhập Email để có nhận liên kết thay đổi mật khẩu </p>
<form asp-action="ForgotPassword" method="post">
    <div class="form-group">
        <label asp-for="Email"></label>
        <input asp-for="Email" class="form-control" />
        <span asp-validation-for="Email" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Gửi</button>
</form> *@
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">

<link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet">
<div class="form-gap" style="padding-top:100px">
<div class="container">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="text-center">
                        <h3><i class="fa fa-lock fa-4x"></i></h3>
                        <h2 class="text-center">Quên mật khẩu?</h2>
                        <p>Bạn cần thay đổi mật khẩu của bạn tại đây.</p>
                        <div class="panel-body">

                            <form asp-action="ForgotPassword" role="form" autocomplete="off" class="form" method="post">

                                    <div class="form-group" style="padding-top:30px">
                                    <div class="input-group">
                                        <span class="input-group-addon"><i class="glyphicon glyphicon-envelope color-blue"></i></span>
                                        <input asp-for="Email" placeholder="Email" class="form-control" type="email">
                                        <span asp-validation-for="Email" class="text-danger"></span>
                                    </div>
                                </div>
                                    <div class="button-container" style="padding-top:40px">
                                        <input name="recover-submit" class="btn btn-lg btn-primary" value="Xác nhận" type="submit">
                                        <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-lg btn-warning">Đăng nhập</a>
                                    </div>

                                <input type="hidden" class="hide" name="token" id="token" value="">
</form>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</div>
<style>
    .button-container {
        
        display: flex;
        gap: 10px; /* Khoảng cách giữa các nút, bạn có thể thay đổi giá trị này */
        justify-content: center; /* Canh giữa các nút trong container */
    }

        .button-container .btn {
            flex: 1; /* Để các nút có kích thước bằng nhau, nếu muốn */
            text-align: center; /* Canh giữa nội dung nút */
            display: inline-block; /* Đảm bảo các nút được hiển thị inline-block */
        }
</style>