@model IEnumerable<WebBanHang.ViewModels.MenuLoaiVM>
	<div class="col-lg-12">
                                <div class="mb-3">
                                    <h4>Danh Mục</h4>
                                    <ul class="list-unstyled fruite-categorie">
                                    @foreach(var item in Model){
                                    
                                            <li>
                                                <div class="d-flex justify-content-between fruite-name">
                        <a href="/Product1/DisplayProducts?categoryId=@item.IdCategory"><i class="fas me-2"></i>🎁 @item.NameCategory</a>
                                                    <span>(@item.QuantityCategory)</span>
                                                </div>
                                            </li>

                                        }
                                            
                                    </ul>
                                </div>
                            </div>